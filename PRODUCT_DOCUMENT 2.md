这份深化版的 PRD（产品需求文档）将从“概念描述”下沉到**“执行细节”与“技术实现规范”**，彻底摆脱笼统感。

文档结构按照实际工程落地的逻辑，深度整合了 Chrome 插件抓取、Serverless 部署、向量检索等底层技术，并以高频的电商招聘场景（如渠道运营、主播、电商行政等岗位）作为业务跑通的切入点。

---

# 猎头公司管理智能系统产品文档 (v2.0 深度重构版)

## 1. 产品概述与演进策略

### 1.1 产品定位
不仅仅是 CRM 或 ATS（候选人追踪系统），而是**“业务增强型智能引擎”**。通过端侧（浏览器插件）与云侧（大模型与数据计算）的协同，彻底消除招聘过程中的“复制粘贴”动作，实现人岗匹配的秒级响应。

### 1.2 MVP（最小可行性产品）策略
首期不追求大而全的全端系统，而是聚焦**“单点穿透”**：
* **核心链路**：浏览器插件捕获简历 -> AI 解析并抽取标签 -> 存入向量数据库 -> 依据 JD 自动生成触达话术。

---

## 2. 核心系统架构 (底层到应用层)

### 2.1 基础设施与部署架构 (Infrastructure)
为了应对招聘早晚高峰的流量激增（如批量发送打招呼消息、集中解析大量简历）及控制初期服务器成本，系统采用**Serverless（无服务器）容器化部署**。
* **计算层**：核心 API 服务与后台异步任务全面部署于 Google Cloud Run。利用其根据 HTTP 请求自动扩缩容（Scale-to-zero）的特性，平衡性能与账单成本。
* **异步队列**：使用 Cloud Pub/Sub 或 Redis Queue，将耗时的 AI 报告生成、批量邮件发送等任务与前端主线程解耦，确保 UI 响应时间 `< 200ms`。

### 2.2 数据架构 (Data Layer)
放弃单一关系型数据库，采用**混合存储模型**：
* **结构化业务数据**：PostgreSQL/MySQL，存储企业信息、账号权限、合同流水、字典表（如行业分类）。
* **非结构化文档与搜索**：Elasticsearch (ES) 用于全文关键词检索；**同时引入 Milvus 或 Qdrant（向量数据库）**，将简历文本与 JD 转化为高维向量（Embeddings），实现“语义级”的人才召回（例如：即便简历里没写“女装电商”，但写了“小红书穿搭图文爆款打造”，系统也能通过向量相似度将其匹配给服饰类运营岗）。

### 2.3 采集与反爬架构 (Acquisition Layer)
传统后端 Puppeteer 爬虫存活率极低。数据采集全面转向**“端云结合”**的 RPA 模式。
* **核心组件**：定制化 Google Chrome 浏览器扩展（Extension）。
* **工作机制**：当招聘专员登录 BOSS 直聘、猎聘等平台时，插件在后台运行（Content Script）。通过解析 DOM 树或拦截前端 XHR 响应，直接在本地提取候选人结构化数据，并携带专员的有效 Cookie 与后端 API 交互。这不仅合规利用了本地正常登录态，更彻底规避了集中 IP 抓取被封禁的风险。

---

## 3. 核心功能模块详细设计

### 3.1 极速采集与触达引擎 (基于 Chrome 插件)
本模块是系统的“触角”，负责解决人才入库与激活的第一步。

* **3.1.1 伴随式简历解析**
  * **交互设计**：用户在招聘网站浏览简历时，插件在屏幕右侧滑出悬浮面板。
  * **功能逻辑**：实时读取当前页面的简历信息，调用后端 GLM-4/GPT 接口，瞬间提取出标准化 JSON 格式（包含：核心技能栈、最高学历、平均跳槽频率、当前薪资预估）。
  * **一键查重**：提取候选人手机号/邮箱或姓名+经历的哈希值，与内部人才库比对，防止重复录入或撞单。

* **3.1.2 场景化 AI 自动打招呼**
  * **业务痛点**：千篇一律的“您好，有兴趣看新机会吗”回复率极低。
  * **功能逻辑**：插件读取候选人过往经历，结合当前推广的岗位，自动生成 **3 个版本的定制化话术**供专员一键发送。
    * *示例*：“看您之前负责过拼多多渠道的品牌女装运营，产出数据很亮眼。我们目前手里有一个同样是女装类目的盘子，急需您这样懂 PDD 投产比的操盘手，预算空间很大，有兴趣聊聊吗？”

### 3.2 智能交付管理与 RAG 匹配模块
本模块是系统的“大脑”，负责精准的“人岗匹配”。

* **3.2.1 JD 智能增强与逆向拆解**
  * **功能描述**：HR 输入一段简陋的岗位需求，系统通过大模型自动补全隐含条件。
  * **能力画像映射**：将文本拆解为必须项（Must-have）和加分项（Nice-to-have），自动映射到系统的技能图谱库中。

* **3.2.2 检索增强生成 (RAG) 推荐报告生成器**
  * **系统机制**：不再是让 AI 凭空写推荐信。而是先从本地库检索该企业过往发过 Offer 的候选人特征（Ground Truth），将这些特征作为 Prompt 的一部分喂给大模型。
  * **输出物**：生成结构化的 PDF/在线报告。
    * 【硬性匹配度】：92%（电商行政经验、熟练使用某 ERP 系统）。
    * 【风险预警】：过去 3 年内有两段工作经历不足 6 个月。
    * 【面试建议提问】：建议针对上一份工作离职原因、以及面对双十一大促期间高压排班的适应能力进行提问。

### 3.3 企业级协作与收件箱 (UI/UX 层)
本模块是系统的“皮相”，追求极致的操作效率。

* **3.3.1 分屏沉浸式工作台 (Split-View Workspace)**
  * **左侧区**：动态候选人队列（卡片式，展示姓名、标签、当前流程节点）。
  * **右侧区（核心操作区）**：支持快捷键操作。
    * `Space` 键：预览简历原件。
    * `Enter` 键：弹出快速备注/跟进弹窗。
    * 拖拽卡片：在 Kanban（看板）视图中拖拽候选人至“推给客户”、“安排面试”、“发送 Offer”列，自动触发底层 Webhook（如：拖入“安排面试”，系统自动通过飞书/钉钉向企业 HR 发送面试协调卡片）。

* **3.3.2 聚合会话中心 (Omnichannel Inbox)**
  * **功能描述**：整合微信（企业微信 API）、邮件、招聘平台私信，在同一个界面内处理所有候选人和客户的消息。
  * **侧边栏外挂**：在聊天窗口右侧，永远悬浮当前聊天对象的 360° 视图（Ta 投递了什么岗位、当前的期望薪资、上一次是谁在跟进）。

---

## 4. 接口与数据字典规范 (示例)

为了保证前后端分离开发的顺畅，核心对象的数据结构需要严格定义。

**对象：Candidate (候选人)**
```json
{
  "candidate_id": "uuid",
  "basic_info": {
    "name": "string",
    "gender": "enum(male, female)",
    "contact": {
      "phone": "string",
      "wechat": "string",
      "email": "string"
    }
  },
  "vector_embedding": "[float]", // 用于向量检索的 1536 维向量
  "parsed_tags": {
    "industry": ["women_apparel", "e_commerce"],
    "roles": ["channel_operation", "admin"],
    "seniority_years": 5.5,
    "stability_score": 85 // AI 评估的稳定性分数
  },
  "source": {
    "platform": "boss_zhipin",
    "imported_by_extension_version": "v1.2.0"
  }
}
```

---

## 5. 项目推进与 MVP 迭代路线图

### Phase 1: 效率工具跑通 (Month 1-2)
* **核心目标**：实现内部专员提效，取代手工录入。
* **开发重点**：
  * 完成 Chrome 浏览器插件的 DOM 解析与数据回传功能。
  * 搭建 Google Cloud Run 后端环境与数据库基建。
  * 跑通简历入库、解析标签、列表展示的闭环。

### Phase 2: 智能匹配与 AI 引入 (Month 3-4)
* **核心目标**：实现“机器帮人找人”。
* **开发重点**：
  * 接入大模型 API，建立 RAG 工作流。
  * 部署向量数据库，实现基于 JD 的候选人智能排序与召回。
  * 开发一键生成“标准化人才报告”功能。

### Phase 3: 客户协同与商业化 (Month 5-6)
* **核心目标**：打通外部 HR，实现服务交付闭环。
* **开发重点**：
  * 飞书/钉钉开放平台深度集成（消息推送、审批流）。
  * 研发 B 端客户看板（HR 端），客户可直接在系统内评价简历、确认面试时间。
  * 完善账单、合同与回款统计图表。